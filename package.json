{
  "name": "deslopper",
  "version": "0.2.0",
  "private": true,
  "description": "Dev and release scripts for the deslopper Python package.",
  "scripts": {
    "bump-release": "bash scripts/preflight.sh && ver-bump -c -p origin && git push --follow-tags && gh release create \"v$(npm pkg get version | tr -d '\\\"')\" --generate-notes"
  }
}
