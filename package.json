{
  "name": "deslopper",
  "version": "0.2.0",
  "private": true,
  "description": "Dev and release scripts for the deslopper Python package.",
  "scripts": {
    "bump-release": "bash scripts/preflight.sh && ver-bump -c && bash scripts/postflight.sh && git push --follow-tags && gh release create \"v$(npm pkg get version | tr -d '\\\"')\" --generate-notes",
    "screenshots": "bash dev/screenshots.sh",
    "screenshots:help": "bash dev/screenshots.sh help",
    "screenshots:demo": "bash dev/screenshots.sh demo"
  }
}
